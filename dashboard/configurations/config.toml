
dir_path = "D:/shared/data" #/data

[app]
app_name = "DashboardApp"
auth = "StagePFA"

[exe]
name_of_python_exe = "server_dashboard"

[config]
config_data_name = "config_data.toml"

[log]
log_file_desktop_app = "logs/{ts}_dashboard_desktop_app.log"
log_file_server = "logs/{ts}_dashboard_server.log"

[redis]
host = "localhost"
port = 6379
db   = 0

[database]
driver = "postgresql+psycopg2"
host   = "localhost"
port   = 5432
name   = "ram_dashboard"
user   = "ram_user"
# no password here in Git-tracked files